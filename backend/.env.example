# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# =============================================================================
# These must be set for the backend to start successfully.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Do NOT commit .env to version control. It contains sensitive secrets.
# The .env file is already in .gitignore.

# Database Connection (REQUIRED)
# PostgreSQL connection string for Supabase or your PostgreSQL database
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database

# OpenAI API Key (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# JWT Secret (REQUIRED)
# Secret key for signing JWT tokens. Use a strong random string in production!
# Generate one with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-key-here

# =============================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# =============================================================================
# These have defaults or are optional features.

# Frontend URL (optional - has default)
# Backend URL for OAuth redirects and CORS
REACT_APP_BACKEND_URL=http://localhost:8010

# CORS Origins (optional - defaults to '*')
# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Environment Mode (optional - defaults to 'development')
# Set to 'production' to disable .env file loading and API docs
ENV=development

# Google OAuth (optional - only needed for Google login)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
