# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update with your backend URL.
#
# IMPORTANT: Do NOT commit .env to version control. It may contain sensitive URLs.
# The .env file should already be in .gitignore.

# Backend API URL (REQUIRED)
# The base URL of your backend API server
# For local development, this is typically: http://localhost:8010 or http://127.0.0.1:8010
REACT_APP_BACKEND_URL=http://localhost:8010
